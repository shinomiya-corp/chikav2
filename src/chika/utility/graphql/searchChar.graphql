query character($charName: String) {
  Character(search: $charName) {
    name {
      full
      native
    }
    image {
      large
    }
    description(asHtml: true)
    age
    gender
    dateOfBirth {
      year
      month
      day
    }
    siteUrl
  }
}
